[effect_script*]
{
	name = summon_gargoyle;  // the original effect is called gargoyle_summon so we're good

	// Shifted 1m up because reference point of gargoyle is on ground
	script =[[
		set $up 1.0;
		call summon "[0]offset(0,$up,0)";
	]];
}
