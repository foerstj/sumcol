[effect_script*]
{
	name = un_summon_vines_killer;

    // Shifted 2m up because reference point of dsx_vines_killer is below ground
	script =[[
		set $up 2.0;
		call un_summon "[0]offset(0,$up,0)";
	]];
}
